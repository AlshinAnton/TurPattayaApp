<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.turpattaya.turpattayaapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true">
        <meta-data
            android:name="android.app.searchable"
            android:resource="@xml/searchable" />

        <activity
            android:name=".excursions.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>
        <activity
            android:name=".excursions.ExcursionDetailActivity"
            android:label="@string/title_activity_excursion_detail"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity android:name=".taxi.OrderForm" />
        <activity android:name=".taxi.TaxiActivity" />
        <activity
            android:name=".BaseActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity
            android:name=".currencyExchange.CurrencyExchangeActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity android:name=".currencyExchange.CurrencyConverter">
            android:theme="@style/Theme.AppCompat.NoActionBar"
        </activity>
        <activity android:name=".weather.WeatherActivity" />
        <activity android:name=".weather.SettingsWeatherActivity" />
        <activity android:name=".MyFavoriteActivity" />
        <activity android:name=".ShareActivity" />
        <activity android:name=".ContactsActivity" />
        <activity android:name=".FreeTransfersActivity"></activity>
    </application>

</manifest>